function solution(citations) {
    var answer = 0;
    const c = citations.sort( (a,b)=> a-b);
    const half = c[parseInt(c.length/2)];
    let val = c[half];
    // 생각해보면 이상 인용의 길이로 계산하면 됐음. 나중에 다시 보기
    for
}
